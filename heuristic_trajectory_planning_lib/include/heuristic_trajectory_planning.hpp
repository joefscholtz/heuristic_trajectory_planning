#include "OptimizationConfig.hpp" // Generated by quicktype
#include "yaml_to_json.hpp"
#include <iostream>

void load_config(const std::string &yaml_file_path) {
  YAML::Node root = YAML::LoadFile(yaml_file_path);
  nlohmann::json j = htp::config::yaml_to_json(root);
  auto config = j.get<OptimizationConfig>();

  // std::cout << "Iterations: " << config.iterations.value_or(0) << "\n";
  // If you implemented the Factory pattern, you can extract the opaque model
  // block: auto model_payload = j["model"]; BaseModel* model =
  // ModelFactory::create(model_payload["type"], model_payload);
}
